/// <reference types="expo/types" />

// Expo 가 제공하는 전역 타입(이미지/CSS 등 에셋 import, 환경 변수)을 불러옵니다.
// 같은 참조가 담긴 `expo-env.d.ts` 는 dev 서버를 띄워야 생성되고 gitignore 대상이라,
// CI 에서 `tsc --noEmit` 이 바로 돌 수 있도록 이 파일을 커밋해 둡니다.
