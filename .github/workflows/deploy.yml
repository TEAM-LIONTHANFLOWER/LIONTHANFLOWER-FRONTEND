name: Deploy to Cloudflare Pages

# main/develop push 시 Cloudflare Pages 로 배포.
# 시크릿(CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID)이 설정돼 있을 때만 동작한다.
# 시크릿 추가 전에는 guard 잡이 deploy 를 skip 하므로 레포가 빨간불이 되지 않는다.
#
# 설정 방법:
#  1. Cloudflare 대시보드 → Pages 에서 프로젝트 생성 (이름: mcm-orbit).
#     아래 deploy 스텝의 --project-name 값과 반드시 같아야 한다.
#  2. GitHub repo → Settings → Secrets and variables → Actions 에 등록:
#     - CLOUDFLARE_API_TOKEN        (Pages 편집 권한 토큰)
#     - CLOUDFLARE_ACCOUNT_ID
#     - EXPO_PUBLIC_API_BASE_URL    (백엔드 API URL, 빌드 타임에 번들로 인라인됨)

on:
  push:
    branches: [main, develop]

jobs:
  guard:
    name: Check secrets
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ steps.check.outputs.ready }}
    steps:
      - id: check
        run: echo "ready=${{ secrets.CLOUDFLARE_API_TOKEN != '' && secrets.CLOUDFLARE_ACCOUNT_ID != '' }}" >> "$GITHUB_OUTPUT"

  deploy:
    name: Build & Deploy
    needs: guard
    if: needs.guard.outputs.ready == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build:web
        env:
          EXPO_PUBLIC_API_BASE_URL: ${{ secrets.EXPO_PUBLIC_API_BASE_URL }}

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=mcm-orbit --branch=${{ github.ref_name }}
